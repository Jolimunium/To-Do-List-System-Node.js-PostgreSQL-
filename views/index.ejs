<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>
    <%- include('partials/navbar') %>

    <div class="d-flex justify-content-center align-items-center" style="min-height: calc(100vh - 56px);">
        <div class="card shadow p-4" style="width: 550px;">
            <% if (typeof user !== 'undefined') { %>
                <h3 class="text-center mb-3">You are logged in</h3>
                <h3 class="text-center mb-3">Your ID: <%= user.id %></h3>
                <h3 class="text-center mb-3">Your Username: <%= user.username %></h3>
                <h3 class="text-center mb-3">Your Email: <%= user.email %></h3>
            <% } else { %>
                <h3 class="text-center mb-3">You are not logged in</h3>
            <% } %>
        </div>
    </div>
</body>

</html>